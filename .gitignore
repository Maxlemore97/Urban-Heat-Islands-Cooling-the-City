# ---- project-level ignores ---------------------------------------------
# LaTeX build artefacts are handled by Report_LaTeX/.gitignore.

# Course handouts and third-party reference PDFs: ~160 MB of material we did
# not write and may not redistribute. Kept locally, out of the repository.
Course material/
Referenz Material/

# Generated simulation animations. Reproducible with
#   cd Output && python3 fetch_weather.py && python3 house_render.py
# The still figures next to them are tracked, because the report may cite them.
Output/wall_model/*.gif

# ---- editors / OS -------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp
__pycache__/
*.pyc

# ---- Proton Drive sync artefacts ---------------------------------------
*# Edit conflict *#*
*.BACKUP.docx
*.BACKUP2.docx

# ---- scratch ------------------------------------------------------------
*.tmp
